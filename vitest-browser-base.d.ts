/**
 * Options Vitest Browser Mode (Playwright provider).
 * Type volontairement libre pour éviter les conflits cross-version vitest.
 */
export const baseBrowserTestOptions: Record<string, unknown>;
