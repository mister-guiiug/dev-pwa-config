## Ce que ça change

<!-- Le comportement observable, pas la liste des fichiers. -->

## Pourquoi

<!-- Le problème constaté. Un lien vers l'issue, ou ce qui a été mesuré. -->

## Vérifications

- [ ] `npm run validate` passe (format, lint, types, tests)
- [ ] Un test échoue **sans** le correctif, et passe avec
- [ ] `npm run sync` relancé si un fichier engendré est concerné
- [ ] Changeset ajouté (`npm run changeset`) si le contenu **publié** change

## Impact sur les apps consommatrices

<!-- Seize dépôts consomment ce paquet. Rupture d'API ? Migration à prévoir ?
     « Aucun » est une réponse valable — mais elle doit être écrite. -->
