/**
 * Déclarations d'appoint pour la vérification de types DE CE DÉPÔT.
 *
 * Non publié : ces modules n'exposent pas de types, et le paquet ne réexporte
 * pas leurs formes. Sans ça, `tsc` refuse de vérifier `eslint-react.js` pour
 * une raison qui n'a rien à voir avec notre code.
 */
declare module 'eslint-plugin-jsx-a11y';
