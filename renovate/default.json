{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Préréglage Renovate de la famille mister-guiiug. Les apps l'étendent : github>mister-guiiug/dev-wpa-config//renovate/default.json",
  "extends": ["config:recommended", ":semanticCommits", ":dependencyDashboard"],
  "timezone": "Europe/Paris",
  "schedule": ["before 7am on saturday"],
  "labels": ["dependencies"],
  "rangeStrategy": "bump",
  "prConcurrentLimit": 4,
  "packageRules": [
    {
      "description": "Le socle : une PR à lui seul, dès qu'il sort, jamais groupé avec le reste.",
      "matchPackageNames": ["@mister-guiiug/dev-wpa-config"],
      "groupName": "socle dev-wpa-config",
      "schedule": ["at any time"]
    },
    {
      "description": "Mineur + patch npm groupés en une PR (moins de bruit).",
      "matchManagers": ["npm"],
      "matchUpdateTypes": ["minor", "patch"],
      "matchPackageNames": ["!@mister-guiiug/dev-wpa-config"],
      "groupName": "npm (mineur & patch)"
    },
    {
      "description": "Peers = stack cible famille : élargir le range, PR séparées, majors revus à la main.",
      "matchDepTypes": ["peerDependencies"],
      "rangeStrategy": "widen"
    },
    {
      "description": "Actions GitHub groupées ; le pin SHA (lighthouse-ci-action) est maintenu à jour.",
      "matchManagers": ["github-actions"],
      "groupName": "github-actions"
    }
  ]
}
